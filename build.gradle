plugins {
  id("application")
}
repositories {
  mavenCentral()
}
dependencies {
  implementation("com.slack.api:bolt-jetty:1.16.0")
  implementation("org.slf4j:slf4j-simple:1.7.33")
}
application {
  mainClassName = "com.jaglyser.SlackBot"
}
run {
  // gradle run -DslackLogLevel=debug
  systemProperty "org.slf4j.simpleLogger.log.com.slack.api", System.getProperty("slackLogLevel")
}